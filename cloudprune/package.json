{
  "name": "cloudprune",
  "version": "0.1.0",
  "private": true,
  "description": "CloudPrune cloud cost monitoring and savings workspace.",
  "scripts": {
    "assess:aws": "node scripts/aws-assessment.js",
    "start": "node server.js",
    "check": "node --check server.js && node --check cloudprune/app.js && node --check scripts/aws-assessment.js && node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.22.0"
  }
}
